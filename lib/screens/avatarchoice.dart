/*import 'package:flutter/material.dart';

class AvatarPage extends StatelessWidget {
  AvatarPage({Key? key}) : super(key: key);

  static const route = '/avatarpage/';
  static const routename = 'AvatarPage';

  @override
  Widget build(BuildContext context) {
    print('${AvatarPage.routename} built');
    return Scaffold(
      appBar: AppBar(
        title: const Text(AvatarPage.routename),
      ),
      body: Center(
        child: Column(
          children: <Widget> [
              Row(children: [
                  Expanded(child: Image.)
              ]
              )
        ],
        ),
        ),
        );
  }
  }*/
